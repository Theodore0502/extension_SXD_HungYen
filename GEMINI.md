# Quy Định Dự Án (Project Rules)

## Quy Định Đánh Phiên Bản (Versioning Rule)
- Mỗi lần cập nhật/sửa đổi code trong dự án này, bắt buộc phải tăng **+1 vào số đuôi patch** của version (theo chuẩn Semantic Versioning: `MAJOR.MINOR.PATCH`).
- File cần cập nhật phiên bản:
  - [manifest.json](file:///f:/extension%20HY/manifest.json): Trường `"version": "x.y.z"` (Ví dụ: `1.0.0` -> `1.0.1`).
- Mọi tài liệu hoặc UI có hiển thị version cũng cần đồng bộ theo số version mới này.
